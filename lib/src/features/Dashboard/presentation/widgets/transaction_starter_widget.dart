import 'package:flutter/material.dart';

class TransactionStarterWidget extends StatelessWidget {
  const TransactionStarterWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
