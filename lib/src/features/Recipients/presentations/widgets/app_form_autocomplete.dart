import 'package:flutter/material.dart';

class AppFormAutocomplete extends StatelessWidget {
  const AppFormAutocomplete({super.key, required this.options});

  final List options;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
